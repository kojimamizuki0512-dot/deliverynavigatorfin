{
  "name": "deliverynavigatorfin-frontend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18 <21" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port ${PORT:-8080}",
    "clean": "rimraf node_modules/.cache node_modules/.vite .vite .cache dist || true"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "rimraf": "^6.0.1",
    "vite": "^5.4.8"
  }
}
